steps taken :
created new github repo at github site (maybe should have done at terminal)
created local repo:
    mkdir basic_repo
    touch index.html styles.css (add some content as well)
    git init
    git add .
    git commit -m "initial commit"
push to remote repo
    git remote add origin https://github.com/alm958/basic_repo
    git push -u origin master
added p-tag and updated
    git add index.html
    git commit -m "added ptag to html"
    git push -u origin master
    
